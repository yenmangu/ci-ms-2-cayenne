/**
 *
 * @returns {string}
 */
export function renderNoResults() {
	return `<section class="no-results">
		<div class="container">
			Sorry, we could not find any recipes matching those search parameters.
		</div>
	</section>
	`;
}
