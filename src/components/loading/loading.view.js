/**
 * Renders the loading component to the DOM
 * @returns {string}
 */
export function renderLoading() {
	return `<div class="container my-4 loading-container">
	<h2 class="loading-heading">Loading...</h2>
</div>`;
}
